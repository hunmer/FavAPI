# FavAPI Web 管理界面 — 页面功能导览

FavAPI 是一个私有化部署的多平台个人收藏抓取服务，Web 管理界面覆盖了从「登录账号 → 触发抓取 → 查看数据」的完整闭环。界面顶部提供三个一级入口：**账号**、**任务**、**数据**，分别对应账号管理、执行记录和数据浏览。

---

## 账号管理（首页）

管理你在各平台的登录身份，是使用 FavAPI 的起点。

- **创建账号**：选择目标平台（未接入的平台会标注「即将支持」并不可选），为账号起一个好认的名字（如「我的抖音主号」），即可创建。
- **扫码登录**：点击「登录」后服务会打开一个真实浏览器窗口，用平台 App 扫码即可完成登录。页面会自动轮询登录进度，扫码成功或超时都会即时提示，无需手动刷新。
- **账号总览**：每个账号展示当前状态（已启用 / 登录过期 / 已禁用 / 登录中）、最后登录时间和最后使用时间，点击账号名称可进入详情页。

## 账号详情

单个账号的完整工作台，账号信息、登录态维护、手动抓取都在这里完成。

### 账号信息与登录态

- **信息总览**：账号 ID、名称、平台、登录/使用记录、浏览器 Profile 位置一目了然；Bilibili 账号还会展示收藏夹主人的头像昵称，以及名下所有收藏夹和各自的收藏数量。
- **登录态检查**：一键验证当前登录是否仍然有效，结果会自动同步到账号状态。
- **重新登录**：登录过期后随时可以重新打开登录窗口扫码续期。
- **Cookies 查看**：读取当前浏览器会话的 Cookies 并展示（名称、域、过期时间），同时自动存库备用。
- **打开浏览器**：以该账号的身份打开一个浏览器窗口，可用于手动检查页面环境；再次点击即可关闭。
- **禁用 / 启用**：临时停用账号，被禁用的账号不会参与抓取。
- **删除账号**：删除账号及其收藏关系、浏览器 Profile，操作前有二次确认。

### 手动触发抓取

在页面上直接发起一次收藏抓取，参数按平台自动适配：

- **通用参数**：抓取数量（0 表示抓取全部，此时结果以流式方式实时滚动展示，抓一条看一条）、起始游标（用于翻页续抓）、是否异步执行。
- **Bilibili 专属**：可指定收藏夹主页链接或用户 ID（留空则抓当前登录用户）、指定单个收藏夹（media_id）、调节翻页间隔以平衡速度与风控风险。
- **小红书专属**：可指定个人主页链接或用户 ID（留空则抓当前登录用户）。
- **结果反馈**：抓取完成后展示总数、新增收藏数、收藏夹主人信息，以及每条内容的标题（可点击跳转原站）、作者、时长、所属收藏夹和收藏时间；翻页场景会提示续抓游标。

### 最近任务

展示该账号最近 10 次抓取记录，包含状态、抓取数量和错误信息。

## 任务记录

全局视角的执行历史，所有账号的抓取任务汇聚于此。

- 每条任务记录任务 ID、开始时间、平台、账号、操作类型、执行状态、抓取数量和错误信息。
- 页面每 5 秒自动刷新，执行中的任务进度无需人工干预即可跟进。

## 数据浏览

已入库收藏内容的检索与浏览中心，支持两种视图自由切换。

- **按账号筛选**：可查看全部账号的数据，也可聚焦到某个账号。
- **分页浏览**：每页 20 / 50 / 100 / 200 条可选，支持前后翻页，并显示当前范围与总量。
- **列表视图**：适合逐条核对，展示封面、标题（可点击跳转原站）、作者、时长、点赞数、收藏数、所属收藏夹、收藏时间和抓取时间。
- **网格视图**：类似「刷视频」的封面卡片流，卡片上叠加播放量与时长角标，下方展示作者与点赞/收藏数，适合快速浏览和找内容。
- **视图记忆**：列表/网格的偏好会被记住，下次进入自动恢复。

---

## 典型使用路径

1. **首页**创建账号 → 扫码登录；
2. 进入**账号详情**，按需填写参数后触发抓取，实时查看结果；
3. 随时在**任务记录**回顾执行历史，在**数据浏览**翻阅已入库的收藏内容。
